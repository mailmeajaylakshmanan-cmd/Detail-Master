# Puppeteer needs unzip to extract Chrome during npm install.
# Keep this minimal so Railway apt install succeeds on Ubuntu 24.04+.
[phases.setup]
aptPkgs = ["unzip", "ca-certificates"]

[variables]
PUPPETEER_CACHE_DIR = "/app/.cache/puppeteer"
